import { Breakpoint } from '@material-ui/core/styles/createBreakpoints'

// The breakpoint to stop showing the options (such as filter options) inline and show them below the hero's title
export const expandOptionsBreakpoint: Breakpoint = 'md'
